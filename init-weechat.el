;;; init-weechat.el --- weechat config (still not finished)
;;; Commentary:
;;; Code:

(require 'weechat)

(setq weechat-host-default "localhost")
(setq weechat-port-default 9000)

(setq weechat-notification-mode t)

(provide 'init-weechat)

;;; init-weechat.el ends here
